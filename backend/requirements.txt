# 陪你读 — 后端依赖
# Python 3.11+

# Web 框架
fastapi>=0.115
uvicorn[standard]>=0.30

# 异步 HTTP（检索 / ar5iv / LaTeX 下载）
httpx>=0.27

# HTML 解析（ar5iv 提取）
beautifulsoup4>=4.12
lxml>=5.2

# LaTeX 源码 → 可读文本（备路径）
pylatexenc>=2.1

# 模糊匹配标题
rapidfuzz>=3.9

# 统一 LLM 接口（D8：所有 LLM 调用走 LiteLLM）
litellm>=1.50
# LiteLLM 原生 tool calling 的 JSON 序列化依赖
orjson>=3.10

# 数据校验 / 配置
pydantic>=2.7
pyyaml>=6.0

# 异步 SQLite（元数据存储）
aiosqlite>=0.20

# 表单 / SSE 支持
python-multipart>=0.0.9

# .env 文件加载（本地开发用，启动时自动注入环境变量）
python-dotenv>=1.0
